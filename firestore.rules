rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /ingestas/{document=**} {
      allow read, write: if true;  // For development - update with proper auth rules later
    }
  }
} 